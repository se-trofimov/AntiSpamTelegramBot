name: ci

on:
  push:
    branches:
      - main

jobs:
  deploy-infrastracture-to-dev:
    steps:
      - name: Setup Environment Variables
        uses: ./.github/setup-environment-variables
        with:
          environment: dev

      - name: Check
        run: echo ${{ env.AZ_TERRAFORM_BACKEND_RESOURCEGROUP }}
